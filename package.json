{
  "name": "travel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch styles/main.scss:styles.css --style=expanded --source-map"
  },
  "private": true,
  "devDependencies": {
    "sass": "^1.86.0"
  }
}
